<%= form_for @user, html: { multipart: true } do |f| %>
<%= f.label :Email %>:
<%= f.text_field :email %><br />

<%= f.label "Password:" %>:
<%= f.password_field :password %><br />

<%= f.submit "Submit"%>

<% end %>